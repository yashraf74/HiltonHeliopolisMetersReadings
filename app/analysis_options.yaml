analyzer:
  exclude:
    - build/**
    - android/**
    - ios/**
include: package:flutter_lints/flutter.yaml

linter:
  rules:
    # Private fields initialised from differently-named public parameters are
    # deliberate throughout the state layer.
    prefer_initializing_formals: false
